<UserControl x:Class="Yotify.View.PlaylistsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Yotify.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="#1a1a1a">
    <Grid>
        <ListView ItemsSource="{Binding Playlists}" Background="Transparent">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <Image Source="{Binding ThumbnailURL}" Width="150" />
                        <StackPanel>
                            <Label Content="{Binding Name}" Foreground="White" />
                            <Label Content="{Binding Description}" Foreground="White" />
                            <Label Content="{Binding PlaylistURL}" Foreground="White" />
                            <Label Content="{Binding ThumbnailURL}" Foreground="White" />
                        </StackPanel>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
